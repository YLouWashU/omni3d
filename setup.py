from setuptools import setup, find_packages

setup(
    name="cubercnn",  # Name of your package
    version="0.1",  # Package version
    packages=find_packages(),  # Specify the package directory to include
)
